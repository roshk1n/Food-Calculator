<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="StyledTilEditTextTheme">
        <item name="android:imeOptions">actionDone</item>
        <item name="colorAccent">@color/colorAccentLogin</item>
        <item name="android:textColorHint">@color/colorSingUpHint</item>
        <item name="colorControlNormal">@color/colorSingUpHint</item>
        <item name="colorControlActivated">@color/colorAccentLogin</item>
        <item name="colorControlHighlight">@color/colorSingUpHint</item>
    </style>

    <style name="StyledTilEditText">
        <item name="android:theme">@style/StyledTilEditTextTheme</item>
    </style>
</resources>
